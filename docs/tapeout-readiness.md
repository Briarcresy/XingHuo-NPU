# 流片准备记录

本次补齐的是可在当前工程内执行的前端验证、零延迟门级功能验证、布局前多角时序估算与可追溯导出。尚未取得最终平台的物理签核依据，不能将这些结果作为允许流片的结论。

## 已完成的设计与验证工作

- 修复了手动写入与模式请求同拍发生时丢失已接受写入的问题。失败证据保存在本机`build/review/mode-handover-before-fix.log`，修复后24组模式相对时序回归与SVA通过。
- 模式交接等待已接受的写入、启动、清状态脉冲；模式请求不一致时停止旧模式接收新命令。
- 增加DIP两级同步及同步链属性；明确主机数据稳定窗口、ACK、busy、复位与启动状态的使用约定。
- reset期间屏蔽RAM写使能，四态仿真不初始化Shared RAM，验证固定Demo和复位恢复。
- 增加160组两层Golden Model对比、地址回绕、忙时请求、85个复位相对时刻、24组模式切换边界及完整Tile仿真断言。
- 切换到官方main PDK，使用真实标准单元模型进行零延迟门级仿真；未修改PDK模型。
- 同一RVT网表执行7个库角STA，包含明确标注的探索性IO延迟、负载、转换时间和时钟不确定度。快角hold违例是待物理修复事项。
- 官方check、export、export-check，及包含SHA256的交付包归档流程。

## 本机7角结果（200 MHz，探索性预算）

| RVT库角 | Setup WNS / ns | Hold WNS / ns | 结果 |
| --- | ---: | ---: | --- |
| ff_cbest_1p32_125 | 0.238 | -0.018 | Hold未满足 |
| ff_rcbest_1p08_125 | 0.046 | 0.010 | 满足当前预算 |
| ff_rcbest_1p32_m40 | 0.288 | -0.030 | Hold未满足 |
| ss_cworst_1p08_m40 | -1.240 | 0.102 | Setup未满足 |
| ss_rcworst_1p08_125 | -1.176 | 0.106 | Setup未满足 |
| ss_rcworst_1p2_m40 | -0.186 | 0.035 | Setup未满足 |
| typ_tt_1p2_25 | 0.021 | 0.012 | 满足当前预算 |

该表记录本次验证结果；后续RTL、工具或库更新后以`make release-check`生成的包内报告为准。

## 仍需最终平台提供或确认

| 项目 | 所需资料或工作 | 当前状态 |
| --- | --- | --- |
| 时钟与复位 | 实际频率、复位来源、时钟抖动/偏斜、Tile时钟是否门控 | 仅使用200 MHz探索性假设 |
| Tile物理约束 | 可用面积、长宽比、引脚位置、利用率、供电和路由层 | 未提供 |
| Shared RAM路径 | RAM实现和读写时序、跨Tile边界的路径预算或顶层网表 | 只有异步读/同步写功能契约 |
| CDC/RDC | 外部接口来源、主机时钟、同步链布局约束及工具检查 | 已补RTL与协议约定，未签核 |
| 多角物理STA | 最终角列表、RC提取、OCV策略、CTS后setup/hold修复 | 仅布局前库角估算 |
| 物理验证 | 对应流片批次的DRC/LVS规则、版图和检查报告 | 当前本机PDK检索未发现对应规则文件 |
| 功耗与可靠性 | 最终工作负载活动、布局寄生、IR drop/EM与供电检查 | 仅默认活动率功耗 |
| DFT与硅后测试 | 平台scan/ATPG分工、覆盖率要求、封装与外部可访问引脚 | 未确认；现有功能测试不等于制造测试 |
| 等价与时序门仿 | 综合/后端等价检查、最终SDF与时序回标 | 未执行；当前门级测试为零延迟 |

库角名称中的RC字样不能替代真实布局寄生。快角hold违例应在CTS/布线阶段通过物理延迟修复并重新检查，不能随意放宽hold uncertainty或添加false path。

## 可追溯交付

执行`make release-check`。每次运行生成独立的`build/releases/<UTC时间>/`，包括检查日志、源码快照、官方Tile导出、PPA报告及`manifest.json`，并生成同名tar.gz。
脚本在验证前后比较源码哈希，防止运行中源码变更导致报告与交付不一致。不会自动提交Git、上传或发起流片。
官方导出工具会复制整个输入目录，因此本项目先在`build/official/source-*`准备干净源码目录，再运行官方export/export-check。最终包不得包含`.git`、本地工具、旧build或reference目录；release检查会拒绝这类意外内容。
官方检查完成后立即冻结导出包，避免后续框架目录清理影响归档。若存在项目根目录下的`mpsoc-digital/Makefile`，本项目优先使用该本地模板；否则使用`~/mpsoc-digital`，也可显式设置`MPSOC_DIGITAL`。模板目录必须保留在`.gitignore`中，官方导出只接受`scripts/prepare_official.py`生成的最小源码目录，避免导出器递归复制嵌套仓库。

包内的`frontend_checks_passed`描述工具与功能检查，`prelayout_timing_met`描述探索性约束下的时序结果；`tapeout_signoff`固定为false，因为此脚本不执行物理签核。提交前应由最终平台补齐上表资料和责任分工。
